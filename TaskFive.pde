/*

//5.a:
//The error: println(output) was placed efter } (the variable output was not defined there). 

void setup() {
  MethodOne(); 

}

//The error: println(output) was placed efter } (the variable output was not defined there). 

void MethodOne()
{
  int i = 1000; // You are not allowed to change this line. 
  
  int max = 10;
  
  if (i > max)
  {
    String output = "i is greater than "+max+".";   
    println(output);
  }
}
*/
